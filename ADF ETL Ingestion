Data source to sink
Azure block storage and azure sql
Link both services to pipline
1. Create New data factory
Choose 
-subscription
-resource group
-instance details (name)
-region
-configure git now
--public endpoint
Review and create
Wait on the launch page till you see deplayment is complete. Then click on Got to Resources

Go to Resources>> click on the data factory name> launch studio
Manage icon (briefcase on left side)

Resources for linked services
Azure sql Database/ Server
-server name (create new)
-location
-use sql authentication
-server admin login
-server admin password
-backup storage redundancy = Locally redundant backup storage

Networking connectivity
-public endpoint
-Firewall rules
--Allow Azure services and resources to access this server = yes
--Add current cliend IP address = yes

Connection policy = default


Go to storage accounts> Create a storage account
-subscription 
-resource group
-storage account name
-region = default
-performance = default
-redundancy = default
-advanced = default
-rest of option = default

Once deployment is complete, click on Go to Resource>blob service
-Add new container (private)
-upload csv file

Create empty table in sql databade
search SQL databases> click on database> query editior
-log in
-run create table query, verify with preview

Create table etldemi (
id VARCHAR(100),
first_name VARCHAR(100),
last_name VARCHAR(100),
email VARCHAR(100),
gender VARCHAR(100),
ip_address VARCHAR(100)
)

Back to data factory and inside the factory created
Manage (briefcase icon on left) >linked services
-new
-blob storage
--name
--storage account
--subscription
--test connection
--create

Author tab> add a new dataset> azure blob storage> data type (csv if that's the data type being worked with)
-name
-linked service (previously created)
-filepath
-create

Linked services> new> azure sql db
-name
-server name
-database anme
-auth type
-username
-password
-create

New dataset>azure sql db
-name
linked service
-table name

Pipeline>copy data
-name
-source
-sink (sqltable)
-can custom map
-validate
-debug

Run select star to verify table is populated

Data factory studio> publish pipeline
Add trigger-- schedule trigger

Delete all resourcves I no longer need
